const common = {
    marginTop: 40,
    paddingHorizontal: 40,
    paddingVertical: 10,
};

const view = {
        flex: 1,
        justifyContent: 'flex-start',
    };

export const views = {
    view: Object.assign({}, common, view),
};
